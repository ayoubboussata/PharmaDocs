# Kopieer dit bestand naar .env en vul echte waarden in. Commit NOOIT de echte .env.

# Anthropic Claude API (Fase 2+) — de sleutel hoort in user-secrets of .env, nooit in Git.
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxx

# PostgreSQL (matcht docker-compose.yml)
POSTGRES_USER=pharmadocs
POSTGRES_PASSWORD=pharmadocs_dev
POSTGRES_DB=pharmadocs
